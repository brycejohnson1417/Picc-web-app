# Canonical Implementation Note

This file is no longer the planning source of truth for the live app.

Use [`AI_HANDOFF.md`](/Users/brycejohnson/Documents/New project/Picc-web-app/AI_HANDOFF.md) as the canonical context for:

- repo path
- production domain
- GitHub repo
- Vercel project
- current architecture constraints
- anti-regression rules

Important:

- Do not use old references to `picc-push` repo paths as product architecture guidance.
- Do not use old Leaflet, MapLibre, Carto, or multi-provider map assumptions.
- Do not treat this file as an active roadmap unless it is rewritten to match the current repo state.
